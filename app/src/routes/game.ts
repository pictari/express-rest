import express, {Router} from 'express';

const router: Router = express.Router();

//router.get('/', getRecentGames);
//router.get('/recent', getMostRecentGame);
//router.get('/history/:id', getGameDetails);
//router.get('/history/entries/:uuid', getRecentAccountEntries);
//router.get('/history/ratings/:uuid', getRecentAccountEntries);

export default router;